<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Validation Results</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Validation Results</h1>

  <% if (emailData) { %>
    <h2>Email Validation</h2>
    <p>Email: <%= emailData.email %></p>
    <p>Deliverability: <%= emailData.deliverability %></p>
    <p>Is Valid Format: <%= emailData.is_valid_format.value %></p>
    <p>Is Free Email: <%= emailData.is_free_email.value %></p>
    <p>Is Disposable Email: <%= emailData.is_disposable_email.value %></p>
    <% if (cacheHit) { %> 
      <p><strong>Note:</strong> This result was served from cache.</p> 
      <% } %>
  <% } %>

  <% if (spamData) { %>
    <h2>Spam Check</h2>
    <p>Spam Score: <%= spamData.Score %></p>
    <p>Content Spam Status: <%= spamData.Details.isContentSpam %></p>
    <p>Number of Spam Words: <%= spamData.Details.numberOfSpamWords %></p>
    <% if (spamData.Details.spamWords && spamData.Details.spamWords.length > 0) { %>
      <p>Spam Words: <%= spamData.Details.spamWords.join(", ") %></p>
    <% } %>
    <p>IP Blocked: <%= spamData.Details.isIPBlocked %></p>
    <p>Email Blocked: <%= spamData.Details.isEmailBlocked %></p>
    <p>Content Too Short: <%= spamData.Details.isContentTooShort %></p>
    <% if (cacheHit) { %> 
      <p><strong>Note:</strong> This result was served from cache.</p> 
      <% } %>
  <% } %>
</body>
</html>
